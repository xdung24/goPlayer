ARG TARGETPLATFORM=linux/arm/v7
FROM --platform=${TARGETPLATFORM} golang:1.25-alpine

RUN apk add --no-cache git musl-dev gcc make wget linux-headers pkgconf alsa-lib-dev

# ALSA is installed via apk for ARM targets to avoid compilation issues under QEMU emulation

WORKDIR /workspace
